migrate to new cmocka API
exec after fork?
add section about deps to readme, also list optional deps for test cases and stuff
add actual documentation
recheck auth protocol overall
find asserts that check things which deserve proper error handling, (see *_free functions)
use gcc 4.9's ubsan?
see where overflow checks make sense and implement them, use gcc 5.0's __builtin_*_overflow
use fuzzer
donkeySponge and monkeyDuplex?
investigate compiler optimization problems and prevent bad optimizations
speed up KeccakF, this one takes up the most time
Keccak based rng?
handle TODOs in code
rewrite end to end testcase in python or something
